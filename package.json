{
  "name": "reminders-mcp",
  "version": "1.0.0",
  "description": "MCP server for Apple Reminders — read, create, complete, and delete reminders via JXA",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "just-claude-reminders": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "mcp",
    "apple-reminders",
    "reminders",
    "macos",
    "claude"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
